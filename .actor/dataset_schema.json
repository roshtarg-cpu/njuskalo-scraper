{
  "actorSpecification": 1,
  "fields": {
    "url": {
      "type": "string",
      "title": "URL",
      "description": "Listing URL"
    },
    "title": {
      "type": "string",
      "title": "Title",
      "description": "Listing title"
    },
    "price": {
      "type": "string",
      "title": "Price",
      "description": "Price (HRK/EUR)"
    },
    "location": {
      "type": "string",
      "title": "Location",
      "description": "City/Region"
    },
    "category": {
      "type": "string",
      "title": "Category",
      "description": "Listing category"
    },
    "condition": {
      "type": "string",
      "title": "Condition",
      "description": "Item condition (new/used)"
    },
    "description": {
      "type": "string",
      "title": "Description",
      "description": "Full listing description"
    },
    "seller": {
      "type": "string",
      "title": "Seller",
      "description": "Seller name/username"
    },
    "postedDate": {
      "type": "string",
      "title": "Posted Date",
      "description": "Listing publication date"
    },
    "imageUrl": {
      "type": "string",
      "title": "Image URL",
      "description": "Main listing image"
    },
    "scrapedAt": {
      "type": "string",
      "title": "Scraped At",
      "description": "ISO timestamp of extraction"
    }
  },
  "views": {
    "overview": {
      "title": "Overview",
      "transformation": {
        "fields": ["title", "price", "location", "url"]
      },
      "display": {
        "component": "table",
        "properties": {
          "title": {
            "label": "Title",
            "format": "text"
          },
          "price": {
            "label": "Price",
            "format": "text"
          },
          "location": {
            "label": "Location",
            "format": "text"
          },
          "url": {
            "label": "URL",
            "format": "link"
          }
        }
      }
    },
    "details": {
      "title": "Full Details",
      "transformation": {
        "fields": ["title", "price", "location", "category", "condition", "seller", "postedDate", "url"]
      },
      "display": {
        "component": "table",
        "properties": {
          "title": {"label": "Title", "format": "text"},
          "price": {"label": "Price", "format": "text"},
          "location": {"label": "Location", "format": "text"},
          "category": {"label": "Category", "format": "text"},
          "condition": {"label": "Condition", "format": "text"},
          "seller": {"label": "Seller", "format": "text"},
          "postedDate": {"label": "Posted", "format": "text"},
          "url": {"label": "URL", "format": "link"}
        }
      }
    }
  }
}
